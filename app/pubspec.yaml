name: app
description: Presentation layer of the AGNT code challenge
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  build_runner: ^2.2.0
  data:
    path: ../data
  flutter:
    sdk: flutter
  freezed_annotation: ^2.1.0
  logging: ^1.0.2
  provider: ^6.0.3


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  freezed: ^2.1.0+1

flutter:
  uses-material-design: true
  assets:
    - assets/config.json
