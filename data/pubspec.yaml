name: data
description: Data layer of the AGNT code challenge
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  domain:
    path: ../domain
  flutter:
    sdk: flutter
  http: ^0.13.4
  json_class: ^2.1.2+8
  logging: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.16.0

flutter:
